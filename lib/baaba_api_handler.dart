/// The conventional entrypoint, matching the package name.
///
/// Identical to `package:baaba_api_handler/ts_api_handler.dart`, which
/// predates the rename and keeps working — import either one.
library;

export 'ts_api_handler.dart';
