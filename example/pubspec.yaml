name: baaba_api_handler_example
description: "Runnable example for baaba_api_handler."
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  baaba_api_handler:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
